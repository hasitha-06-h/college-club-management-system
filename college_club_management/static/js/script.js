// static/js/script.js
// Any global JavaScript for your project can go here.

document.addEventListener('DOMContentLoaded', function() {
    // Example: Simple console log to confirm script is loaded
    console.log("College Club Management JS loaded!");

    // You can add interactive elements here, e.g.,
    // - Smooth scrolling for anchor links
    // - Dynamic content loading (e.g., more clubs on scroll)
    // - Form validation that goes beyond Django's built-in validation
    // - Tooltip initialization if using Bootstrap's JS tooltips
    //   var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'))
    //   var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
    //     return new bootstrap.Tooltip(tooltipTriggerEl)
    //   })
});